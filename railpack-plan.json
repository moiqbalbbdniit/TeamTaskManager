{
  "packages": {
    "node": "22"
  },
  "steps": [
    { "name": "install", "cmd": "rm -f package-lock.json || true && npm install --no-audit --no-fund" },
    { "name": "build", "cmd": "npm run build" },
    { "name": "start", "cmd": "npm run start" }
  ]
}
